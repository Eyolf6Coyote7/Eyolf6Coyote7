name: CI — 3D Asset

on:
  pull_request:
    paths:
      - '3d_asset_collaboration/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-dotnet + dotnet format --verify-no-changes when .csproj exists
      - name: dotnet format (placeholder)
        run: echo "Skipped — no .csproj yet"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-dotnet + dotnet test when tests exist
      - name: Unit tests (placeholder)
        run: echo "Skipped — no tests yet"
