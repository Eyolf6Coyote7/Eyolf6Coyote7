name: CI — Docs

on:
  pull_request:
    paths:
      - 'docs/**'
      - '*/docs/**'
      - '**/*.md'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: markdownlint-cli2-action when .markdownlint.yml config exists
      - name: markdownlint (placeholder)
        run: echo "Skipped — no markdownlint config yet"
