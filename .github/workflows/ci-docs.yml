name: CI — Docs

on:
  pull_request:
    paths:
      - 'docs/**'
      - '*/docs/**'
      - '**/*.md'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: markdownlint
        run: echo "TODO: Enable when markdownlint config exists"
        # - uses: DavidAnson/markdownlint-cli2-action@v19
        #   with:
        #     globs: '**/*.md'
