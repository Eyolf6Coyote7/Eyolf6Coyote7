name: CI — Whiteboard

on:
  pull_request:
    paths:
      - 'realtime_ai_whiteboard/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-node + npm run lint when package.json exists
      - name: ESLint + Prettier (placeholder)
        run: echo "Skipped — no package.json yet"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-node + npm test when tests exist
      - name: Unit tests (placeholder)
        run: echo "Skipped — no tests yet"
