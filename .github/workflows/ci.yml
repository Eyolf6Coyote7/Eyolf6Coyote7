name: CI

on:
  pull_request:
    branches: [dev, stable]
  push:
    branches: [dev]

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Check commit messages follow conventional commits
        run: |
          echo "TODO: Add commitlint when projects have package.json"
          echo "For now, this is a placeholder."

  # Uncomment per-project jobs as development progresses:
  #
  # whiteboard:
  #   runs-on: self-hosted
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: cd realtime_ai_whiteboard/backend && npm ci && npm test
  #
  # workflow:
  #   runs-on: self-hosted
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: cd enterprise_workflow_system/backend && ./gradlew test
  #
  # 3d-asset:
  #   runs-on: self-hosted
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: cd 3d_asset_collaboration/backend && dotnet test
