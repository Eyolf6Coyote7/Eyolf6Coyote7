name: CI — Workflow

on:
  pull_request:
    paths:
      - 'enterprise_workflow_system/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-java + ./gradlew ktlintCheck when build.gradle.kts exists
      - name: ktlint (placeholder)
        run: echo "Skipped — no build.gradle.kts yet"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup-java + ./gradlew test when tests exist
      - name: Unit tests (placeholder)
        run: echo "Skipped — no tests yet"
