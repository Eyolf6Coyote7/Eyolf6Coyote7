package com.workflow.config

import org.springframework.context.annotation.Configuration

@Configuration
class GraphQLConfig {
    // DGS auto-configures scalars and schema loading.
    // Add custom scalar registrations here if needed.
}
